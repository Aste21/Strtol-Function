Name your function 'strtol' and place it in the file named 'strtol.c'.

Library function is used, if you compile test_strtol.c alone.
Your function will be used, if you compile both files together.

Use the provided makefile to build two versions of the test program and compare the results.
To do so execute the 'make' command.
Alternatively you can execute the 'make diff' command which displays the differences between the results.
